# 2026-09-20 - Standalone repaired taxi template

## Completed

- Prepared this independent repository from working source commit
  `7223bcab85b9896474258be04dce0e7b08bff6a5`, with fresh Git history.
- Preserved all 44 application, data, infrastructure, and test files after explicit
  LF/CRLF normalization; generalized only the two environment configuration files.
- Added original MIT attribution, snapshot provenance, test requirements, ignore
  rules, setup instructions, and a reusable online-deployment rerun guide.
- Excluded source Git history, local environments, credentials, trained artifacts,
  and the original environment-specific daily incident reports.
- Prepared publication as a private GitHub repository on `main`, matching the
  existing private accelerator's visibility. No collaborators were added.

## Verification

- Copied pipeline suite: **34 passed**, zero failures.
- Copied model and real inference-server WSGI suite: **18 passed**, zero failures.
- Accelerator initializer suite: **20 passed**, zero failures or skips, including
  generation of this actual snapshot followed by both copied regression suites.
- Fresh disposable Python 3.11.14 environment: **88 installed packages compatible**.
- Static validation: **22 YAML**, **8 JSON**, and **10 Python** files parsed across
  the snapshot and touched accelerator inputs; duplicate YAML keys rejected.
- Credential-token and personal-Azure-setting pattern scan: no matches in the
  publication inputs. This is not a comprehensive dependency security assessment.
- Protected original repository: original clean HEAD plus **52 file fingerprints**
  (including Git configuration, HEAD, and index) unchanged before publication.
- Independent initializer review: approved after reproducing and fixing a shallow
  history bypass in the new-project-only safeguard.

## Documentation

- [Project README](https://github.com/alvinea28/taxi-fare-regression/blob/main/README.md)
- [Generic online rerun guide](https://github.com/alvinea28/taxi-fare-regression/blob/main/docs/online-deployment-rerun.md)
- [This report](https://github.com/alvinea28/taxi-fare-regression/blob/main/docs/daily%20work%20report/2026-09-20.md)
- [Accelerator integration guide](https://github.com/alvinea28/own-ml-ops-v2/blob/main/docs/TAXI-TEMPLATE.md)
- [Accelerator report](https://github.com/alvinea28/own-ml-ops-v2/blob/main/docs/daily%20work%20report/2026-09-20.md)

## Remaining setup and limits

Import this repository into Azure Repos as `taxi-fare-regression-template` and use
the updated personal accelerator's `main` branch. Configure unique resource names,
workload-federated service connections, quota, and compute identity/ACR pull access
before a new deployment. GitHub imports and generated projects are not live mirrors.

No Azure resources, jobs, endpoints, or production traffic were changed or checked
in this publication task. No new live application URL or hosted health result is
claimed. Existing source code is preserved, including its documented infrastructure
identity/RBAC prerequisite; this snapshot does not reproduce Azure-side state.

## Follow-up: accelerator branch changed to main

Updated this repository's README and setup links to the personal accelerator's
new default `main` branch, following the user's requested branch promotion. This
repository stays on `main`; its application, configuration, data, and test files
are unchanged. Synchronize the accelerator's `main` branch into Azure Repos and
select it manually for initialization. Original publication test results remain
above; no new Azure deployment or pipeline run is claimed.

## Follow-up: template-first ESv3 compute configuration

- Updated this reusable template before applying the profile to the generated
  `taxi-fare-demo` project. The original working taxi repository is not changed.
- Infrastructure and training now share `Standard_E4s_v3`, Dedicated, 0–4 nodes.
  Batch uses the same size and Dedicated tier at 0–5 nodes; its manual cluster
  YAML is aligned. Online uses `Standard_E2s_v3`, one instance.
- Added shared SKU/scaling variables to both environment configuration files and
  forwarded them through Bicep validation, Bicep deployment, the compute module,
  and the ML pipeline helper calls. Regenerated the checked-in ARM template.
- The combined maximum is 40 ESv3 quota cores per environment: 16 training,
  20 batch, and 4 online including upgrade reserve. Two colocated environments
  consume an 80-core budget before other workloads or deployment overlap.
- The supplied screenshot reports 96 available Dedicated cores. The CLI could
  not independently read the selected subscription's quota; this is a planning
  budget, not a live capacity guarantee. Region, total-core and workspace caps
  still need verification before deployment.
- Added [compute profile and quota instructions](../compute-quotas.md). They cover
  existing-cluster replacement planning, the helper's skip-existing behavior,
  Dedicated versus low-priority quota, service connections, and identity/ACR prerequisites.

Validation: the new compiled-ARM regression detected the stale snapshot before
regeneration; afterward **10 compute-profile tests** and **34 existing pipeline
tests** passed. Bicep compiled successfully; existing storage/registry name-length
warnings are outside this VM-family change. Model code/dependencies and data are
unchanged, so Python model tests were not rerun. No training or Azure deployment
pipeline was queued and no running compute was resized or deleted.
