$schema: https://azuremlschemas.azureedge.net/latest/amlCompute.schema.json
name: batch-cluster
type: amlcompute
size: Standard_E4s_v3
min_instances: 0
max_instances: 5
tier: dedicated
# Manual CLI alternative; the Azure DevOps pipeline creates this cluster through a shared helper.
# Keep these defaults aligned with config-infra-dev.yml and config-infra-prod.yml.
# Configure a supported compute identity and image-pull permissions separately when required.
